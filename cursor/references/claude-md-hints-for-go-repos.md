# 下游仓库 `CLAUDE.md` 提示（Go / 獬豸）

> **用途（skills 真源）**：给 **各 Go 后端仓** 写 `CLAUDE.md` 时的**结构提示**，避免与工作流 `.mdc` 双写。正文请在各仓库按需落地（用户语言、文档路径以项目为准）。

## 建议结构

1. **用户原则**（测试通过、用语、稳健性、遵守 `.cursor/rules` 等——按团队习惯列条）。
2. **獬豸工作流（一条即可）**  
   - 若启用 Cursor 全局规则：**Go 任务须完整遵循 `xiezhi-workflow.mdc`（獬豸工作流）；其中阶段 2（`audit.py`）、5（测试功能 Review）、6（`commit-summary.mdc`）不得跳过**。
3. **改动后文档同步**（下列子项中的 **文件名、路径** 替换为本仓库真实文档）  
   - 数据库：设计说明文档、建表 / 迁移脚本、迁移策略与回滚。  
   - API：Swagger、接口说明、破坏性变更标注。  
   - 业务 / 架构：设计文档、模块与定时任务等。  
   - 配置与部署：环境变量、配置说明、运维步骤与风险。  
   - 用户可见行为：对客 / 运营说明、变更记录。  
   - 暂无文档时：在 PR / 提交说明中记录影响范围，并计划补齐。

勿在 `CLAUDE.md` 中再抄一份完整的阶段 0–6 细则表；**顺序与门禁**以 **`xiezhi-workflow.mdc`** 为准。
