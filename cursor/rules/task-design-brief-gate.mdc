---
name: 阶段 0.5 方案简报门禁（强制）
description: 核心业务改动前必须先完成四步设计或有效豁免；与獬豸阶段 0.5 同一门禁，alwaysApply 保证每条对话可见。
alwaysApply: true
---

# 阶段 0.5 执行门禁（核心，不可默认跳过）

> 与 **`xiezhi-workflow.mdc`** 阶段 **0.5** **同一套要求**。本文 **`alwaysApply: true`**，用于在 Cursor 中**每条对话自动提醒**，防止「直接改代码」。  
> **四步输出模板唯一真源**：`skills` 仓库 **`cursor/references/task-design-brief.md`**（须 **Read**）。

## 何时必须先做 0.5

满足 **任一** 时，**在未完成 0.5 或未按下文作「有效豁免」声明前**，**禁止** 编写或修改业务实现类 Go 代码（见下节「业务代码范围」）：

- 新增或变更 **HTTP API**（handler / 路由 / 请求响应 DTO 契约）
- **新模块**、**跨包 / 跨模块** 依赖或共享表的新增读写
- **task_plan** 新 Phase / 新 Task，或用户明确要求「实现某功能」涉及上述范围
- **表结构 / migration / 业务 Model** 变更驱动的开发

## 触发后硬顺序（不可颠倒）

1. **Read** `cursor/references/task-design-brief.md`（全路径示例：`~/project/skills/cursor/references/task-design-brief.md`）。  
2. 按文件内模板输出 **Step 1（数据结构）**，**等待用户确认** 后再发 Step 2；依次类推。**Step 1 永远不可跳过**。  
3. **仅当** Step 1–4 已按模板完成（或 Step 2/3/4 已按模板声明跳过且用户确认）后，方可改 **业务代码范围** 内文件。

**业务代码范围**（触发 0.5 时受保护）：`internal/**`、`cmd/**` 下的 `handler`、`service`、`repository`、`dto`、领域 `model`、`wire.go` 等 **实现或契约** 类 `.go`（纯测试、仅 `*_test.go` 若与契约无关可单独约定，但 **改 API 仍须 0.5**）。

## 有效豁免（须同轮次白纸黑字')

以下情况可不展开完整四步，但回复中 **必须** 出现一行：**`阶段 0.5 豁免：<理由>`**，且 **不得** 同时做 API / 表结构 / 新模块类变更：

- **纯 bugfix**：极少行、行为与对外契约不变  
- **纯** 格式 / 注释 / 无契约影响的改名  
- **仅** 文档、**仅** 配置注释、**仅** audit 指出的非行为修改  

若实际改动 **已触及** 接口、DTO、表、跨模块调用，**不得**使用豁免。

## 禁止

- **先改代码再补设计**（触发场景下）  
- **不 Read** `task-design-brief.md` 就凭记忆输出「简略四步」代替正式模板  
- 把明显 **功能 / 契约** 改动谎报为豁免  

## 与獬豸一致

违反本节即违反獬豸工作流；任务结束前 **任务收尾检查** 须勾选阶段 0.5 完成，或勾选豁免并 **理由成立**。
