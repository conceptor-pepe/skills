---
name: Xiezhi Workflow / 獬豸工作流（强制）
description: Xiezhi (獬豸), mythical beast that distinguishes right from wrong. Go dev workflow: coding standards → audit → module gates → code review → test review → commit summary. Use when: xiezhi workflow, 獬豸工作流, go dev workflow.
alwaysApply: true
---

# Xiezhi Workflow / 獬豸工作流（强制）

> Xiezhi (獬豸), the beast that discerns right from wrong. 能辨曲直、触不直。本工作流以其把关之能，约束 Go 开发全流程。

编写或修改 Go 代码时，**必须**按以下流程执行，禁止跳过或颠倒顺序。

## 执行顺序（流水线）

```
0. 需求澄清 ──► 1. 编写 ──► 2. 编码审计 ──► 3. [模块审计] ──► 4. Code Review ──► 5. 测试评审 ──► 6. 提交总结
```

> - 阶段 0 对应 **Think Before Coding**（`karpathy-constraints.mdc` 原则 1）。
> - 阶段 1 贯穿 **Simplicity First**（原则 2）和 **Surgical Changes**（原则 3）。
> - 阶段 5→6 之间验证 **Goal-Driven Execution**（原则 4）。

## 各阶段职责

| 阶段 | 职责 | 执行内容 | 对应规则 |
|------|------|---------|----------|
| 0. 需求澄清 | 显式假设，确认理解 | 把假设说出来，列出歧义，该反驳就反驳，困惑就停 | `karpathy-constraints.mdc` 原则 1 |
| 1. 编写 | 遵守编码规范 | 遵循 go-code-standards，Simplicity First，Surgical Changes | `go-code-standards.mdc` + `karpathy-constraints.mdc` 原则 2、3 |
| 2. 编码审计 | 自动检查 | 执行 `audit.py`，不通过则修复 | `go-audit` skill |
| 3. 模块审计 | 按 Gate 审计 | 模块级任务时按 Gate 0→4 执行 | `karpathy-constraints.mdc` + `module-review-gates.mdc` |
| 4. Code Review | 代码评审 | 人工或 code-reviewer 子任务 | — |
| 5. 测试评审 | 测试用例评审 | Code Review 完成后**立即**执行 | `test-functionality-review.mdc` |
| 6. 提交总结 | 验证成功标准，输出变更总结 | 确认目标达成，按格式输出 | `karpathy-constraints.mdc` 原则 4 + `commit-summary.mdc` |

## 阶段 0：需求澄清（强制）

对应 `karpathy-constraints.mdc` **原则 1：Think Before Coding**。

在开始实现之前，必须执行以下步骤：

1. **把假设显式说出来**。不确定的地方，明确讲出来请用户确认，不要默默选一个方案跑。
2. **列出多种解释**。有歧义时不要只选一个，列出可能的理解，让用户选择。
3. **该反驳就反驳**。如果有更简单的做法，直接提出来。
4. **困惑就停**。说不清的地方就停下来要 clarification。

### 自查清单

- [ ] 我是否理解了用户的真实需求（而不是自己猜的那个）？
- [ ] 是否有多个可能的实现方向？我列出来了吗？
- [ ] 是否有更简单的方案？我反驳了吗？
- [ ] 是否有说不清的地方？我停下来问了吗？

### 何时跳过？

琐碎任务（修错别字、单行改动、变量改名）不需要走完整流程，用判断。

## 阶段 1：编写

- 编写前遵守 `go-code-standards.mdc` 全部规范
- 编写时遵循 **Simplicity First**（`karpathy-constraints.mdc` 原则 2）：不做超出需求的功能，200 行能改成 50 行就重写
- 编写时遵循 **Surgical Changes**（`karpathy-constraints.mdc` 原则 3）：每一行改动都要能追溯到用户的需求，不要「改进」不在任务里的邻近代码
- 禁止：无注释公开函数、未处理错误、超 50 行函数、阻塞操作不传 context

## 阶段 2：编码审计（强制）

- 命令：`python3 ~/.cursor/skills/go-audit/scripts/audit.py <本次修改的.go文件>`
- 审计不通过 → **先修复**，再重跑，直到通过
- 禁止：跳过审计、手动声称通过、用其他方式替代

## 阶段 3：模块审计（适用时）

- 对模块做审计/重构时，按 `module-review-gates.mdc` 执行 Gate 0→4
- 前一 Gate 未通过不得进入下一 Gate
- 阶段 3 全程贯穿 **Surgical Changes**（`karpathy-constraints.mdc` 原则 3）

## 阶段 4→5：Code Review 后测试评审（强制）

- Code Review 完成后，**必须立即**执行测试功能 Review
- 禁止：等待用户说「测试功能 review」再执行
- 禁止：询问「是否进行测试功能 review？」

## 阶段 6：提交总结（强制）

执行 `karpathy-constraints.mdc` **原则 4：Goal-Driven Execution** 的验证步骤：

1. 回顾最初的目标：本次任务完成了什么？是否达成？
2. 确认每一行 diff 都能追溯到需求（对应 Surgical Changes 检查）
3. 按 `commit-summary.mdc` 格式输出变更总结
4. 禁止：总结中包含催促提交用语（如「请审计后提交」）

## 规则优先级

1. 本工作流（执行顺序不可违反）
2. 各阶段对应规则（`karpathy-constraints.mdc` 优先级最高）
3. 用户临时指令
