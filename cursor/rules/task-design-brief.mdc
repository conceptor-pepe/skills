---
name: Task Design Brief / 任务方案简报（强制）
description: 每个 Task 编码前必须输出方案简报，用户确认后才可动手。包含架构影响、API 契约、设计决策三部分。
alwaysApply: true
---

# Task Design Brief / 任务方案简报（强制）

> **核心原则**：先对齐方案再写代码。设计文档是契约不是参考，最简方案优先。

## 触发条件

**每个 Task（或一组紧密关联的 sub-tasks）开始编码前**，必须先输出方案简报并等待用户确认。

触发场景：
- task_plan.md 中的每个 Phase/Task 开始实现时
- 用户说「实现 xxx」「开始 xxx」时
- 任何涉及 **新增接口、新增模块、跨模块调用** 的改动

不触发：
- 纯 bugfix（改 1-2 行修复已知问题）
- 纯格式/注释/文档修改
- 审计修复（audit 不通过的修复）

## 简报模板（强制格式）

```markdown
## Task Brief: [task 名称]

### 架构影响

**模块依赖（本次新增/修改的跨模块调用）**

// 用树形图展示本模块对外部模块的依赖
// 只列新增或变更的依赖，已有的不列

**耦合点**

| 耦合 | 说明 | 风险 |
|------|------|------|
| [被耦合的资源/表/字段] | [怎么耦合的] | [可能的风险] |

**模块内部新增/变更**

// 列出新增和变更的文件，一行一个，标明职责

### API 契约（对照设计文档）

| 接口 | 路由 | 关键响应字段 |
|------|------|-------------|
| [接口名] | [METHOD /path] | [字段列表，必须与设计文档一致] |

> ⚠️ 字段名、响应结构必须从设计文档**原文摘取**，不可自行命名。
> 如需偏离设计文档，必须在「待确认」中明确提出。

### 设计决策（仅列有选择的点）

| 决策 | 方案 | 理由 |
|------|------|------|
| [需要做选择的点] | [选择的方案] | [为什么选这个] |

> 只列存在 **两个以上可选方案** 的决策点。
> 对每个决策必须回答：这是满足需求的最简方案吗？

### 待确认

1. [需要用户拍板的问题，编号列出]
```

## 执行规则

### 必须遵守

1. **简报先行**：未输出简报或用户未确认前，**禁止写任何业务代码**
2. **设计文档是契约**：API 字段名、响应结构严格按设计文档，偏差必须显式提出
3. **最简方案优先（YAGNI）**：先问"不加这个行不行"，不照搬其他模块的复杂模式
4. **模式借鉴检查**：借鉴其他模块时，必须确认该模式的前提条件在当前场景是否成立
5. **等待确认**：输出简报后等待用户回复，不可自行假设确认

### 禁止

- **禁止**跳过简报直接写代码
- **禁止**简报中遗漏 API 契约段（有接口变更时）
- **禁止**简报中遗漏架构影响段（有跨模块调用时）
- **禁止**自行发明字段名/路由名而不对照设计文档
- **禁止**在用户未确认的情况下开始编码

### 粒度控制

- **架构影响**：树形图 + 耦合表 + 文件清单，不展开代码细节
- **API 契约**：接口级，列路由和关键字段，不展开请求/响应完整 schema
- **设计决策**：只列有分歧的点，显而易见的实现（CRUD、标准分层）不列
- 整个简报控制在 **30-50 行**以内，用户 1-2 分钟可扫完

## 与工作流的衔接

本规则插入 `xiezhi-workflow.mdc` 的阶段 0 和阶段 1 之间：

```
0. 功能启动 ──► 0.5 方案简报 ──► 1. 编写 ──► 2. 编码审计 ──► ...
```

阶段 0.5 未通过（用户未确认或要求修改）时，禁止进入阶段 1。
