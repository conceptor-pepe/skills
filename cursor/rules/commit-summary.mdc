---
description: 
globs: 
alwaysApply: true
---

# Commit 变更总结输出规范（强制）

每次完成一个 Commit / 任务的所有代码修改后，**必须**按以下格式输出总结，禁止使用纯文本罗列。

## 输出格式

### 1. 标题行

```
**Commit N: <标题>** 已完成。
```

### 2. 变更表格

按逻辑层级从上到下排列，每行一个文件。文件路径用反引号包裹（Cursor 中可点击）。

```markdown
| 层级 | 文件 | 变更说明 | 变更类型 |
|------|------|----------|----------|
| 配置层 | `configs/config.yaml` | 新增 agent 配置段 | 修改 |
| 基础设施 | `internal/infra/auth/jwt.go` | Claims 增加 TokenType 字段 | 修改 |
```

**层级排列顺序**（从上到下）：

1. 配置层 — config, yaml, env
2. 数据层 — SQL migration, schema
3. 基础设施 — auth, middleware, crypto
4. 模型层 — model, constants
5. 仓储层 — repository
6. 服务层 — service
7. 接口层 — handler, DTO, routes
8. 模块注册 — module.go, wire.go, handlers.go
9. 测试 — test files
10. 文档 — docs

**列说明**：
- 层级：上述层级名之一
- 文件：反引号包裹的相对路径（如 `internal/infra/auth/jwt.go`）
- 变更说明：简洁描述该文件改了什么
- 变更类型：新增 / 修改 / 删除

### 3. 补充信息

```markdown
**关键设计点**：
- 要点1（如向后兼容策略）
- 要点2（如安全考量）

**验证结果**：编译通过 ✅ | 测试通过 ✅ | Lint 清洁 ✅
```

验证失败项标 ❌，无关项可省略。

### 4. 提交信息

在验证结果后，输出一段可直接用于 `git commit -m` 的提交信息，格式如下：

```markdown
**提交信息**：
\`\`\`
feat(agent): 简要标题

- 变更点1
- 变更点2
- 变更点3
\`\`\`
```

规则：
- 首行使用 conventional commit 格式：`feat/fix/refactor(scope): 简要标题`
- 正文用 `-` 列表罗列本次修改的功能点，每条不超过一行
- 语言与项目保持一致（中文项目用中文）

### 5. 编码规范审计（自动执行）

每次完成 Go 代码修改后，**必须**自动执行审计脚本，**禁止跳过或手动替代**。

**执行命令**（对本次修改涉及的 .go 文件）：
```bash
python3 ~/.cursor/skills/go-audit/scripts/audit.py <本次修改的file1.go> [file2.go ...]
```

**流程**：
1. 代码编写完成后，收集本次新增/修改的所有 `.go` 文件路径
2. 执行上述命令（静默执行，不向用户展示原始命令和输出）
3. 根据脚本退出码和输出决定下一步：
   - **退出码 0**（全部通过）：在验证结果行追加脚本输出（如 `编码规范审计 ✅ (8/8)`）
   - **退出码 1**（有违规）：**先修复代码**，修复后重新执行脚本，直到通过
4. 只在最终总结中展示一行审计结论，不展示中间过程

### 6. 禁止事项

- **禁止**在总结中包含提交代码的建议或催促用语（如"请审计后提交"、"请你提交"等）
- 总结到提交信息为止，等待用户的下一步指令
