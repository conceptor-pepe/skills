---
name: Go 编码规范自动审计
description: 完成 Go 代码修改后自动执行编码规范审计脚本
alwaysApply: true
---

# Go 编码规范自动审计（全局强制）

## 触发条件

当新增或修改任何 `.go` 文件后，**必须自动执行**审计脚本，**无需等待用户要求**。

## 执行命令

```bash
python3 ~/.cursor/skills/go-audit/scripts/audit.py <本次修改的file1.go> [file2.go ...]
```

如果涉及整个目录：
```bash
python3 ~/.cursor/skills/go-audit/scripts/audit.py --dir <directory>
```

## 强制流程

1. 完成所有 `.go` 文件的编写/修改
2. 收集本次涉及的文件路径，执行审计脚本
3. **退出码 0**（通过）→ 在总结中追加审计结论（如 `编码规范审计 ✅ (9/9)`）
4. **退出码 1**（违规）→ **必须先修复代码**，重新执行脚本，循环直到通过
5. 审计过程静默执行，只在最终总结中展示一行结论

## `audit:allow-no-log` 层级管控

| 层级 | 是否允许 | 说明 |
|------|----------|------|
| **service** | **禁止** | 所有错误分支必须有日志，确保问题可追溯 |
| **handler** | **禁止** | 所有异常分支必须有日志，避免运维盲区 |
| repository | 允许 | 错误向上传播，由 service/handler 层记录 |

## 审计检查项（共 10 项）

| # | 检查项 | 说明 |
|---|--------|------|
| 1 | 公开函数注释 | 所有公开函数必须有 Go doc 注释 |
| 2 | 函数长度<=50行 | 函数体不超过 50 行 |
| 3 | 函数参数<=5个 | 函数参数不超过 5 个 |
| 4 | if嵌套<=2层 | 控制流嵌套不超过 2 层 |
| 5 | 错误处理 | 忽略错误时必须注释原因 |
| 6 | 错误分支日志 | 错误分支必须有日志（仅 repository 层可豁免） |
| 7 | 无反射使用 | 禁止使用 reflect 包 |
| 8 | context传递 | 阻塞操作必须传递 context |
| 9 | 接口最小化 | 接口方法数不超过 10 个 |
| 10 | int64精度保护 | int64 字段 json tag 必须含 `,string`；[]int64 须用 []types.StringID |

## 禁止事项

- **禁止**跳过审计脚本执行
- **禁止**手动声称"审计通过"而不实际执行脚本
- **禁止**用其他检查方式替代 `audit.py`
- **禁止**在 service 层或 handler 层使用 `audit:allow-no-log` 豁免
