---
name: Karpathy 行为约束（强制）
description: Agent 编码时的 4 条行为约束，基于 Andrej Karpathy 推文归纳。适用于所有非琐碎任务，禁止跳过。
globs: **/*.go
alwaysApply: true
---

# Karpathy 行为约束（强制）

> 来源：Andrej Karpathy 推文吐槽 LLM 写代码的 4 个共性问题，由 forrestchang 归纳为约束规则。
>
> 原文三段话：
> - "The models make wrong assumptions on your behalf and just run along with them without checking..."
> - "They really like to overcomplicate code and APIs, bloat abstractions, don't clean up dead code..."
> - "They still sometimes change/remove comments and code they don't sufficiently understand..."
>
> 本文件把这三段话拆成 4 条可执行原则，融入 xiezhi 工作流。

---

## 问题 → 原则对照表

| 编号 | 问题（Karpathy 原话） | 对应原则 |
|------|----------------------|---------|
| P1 | 不确认就瞎猜：做错误假设、不管理自己的 confusion、不提矛盾、不反驳 | Think Before Coding |
| P2 | 写得过于复杂：overcomplicate、bloat abstractions、100 行写成 1000 行 | Simplicity First |
| P3 | 乱改周边代码：顺手改不相关的注释和代码 | Surgical Changes |
| P4 | 没有成功标准：拿着模糊指令就开跑，做完不知道是不是真的做完 | Goal-Driven Execution |

---

## 原则 1：Think Before Coding（先想清楚再写）

**针对问题 P1：模型会替用户做假设，然后带着这个假设直接跑，不检查、不确认、不提矛盾、该反驳的时候也不反驳。**

### 子规则

1. **把假设显式说出来**。不确定的地方，在实现前明确讲出来，请用户确认，不要默默选一个方案跑。
2. **列出多种解释**。有歧义的时候，不要只选一个，列出可能的理解，让用户选择。
3. **该反驳就反驳**。如果有更简单的做法，直接提出来，不要用户要什么就照做。
4. **困惑就停**。说不清的地方就停下来要 clarification，不猜。

### 执行时机

在 `xiezhi-workflow.mdc` 阶段 1「编写」**之前**执行。

### 自查清单

- [ ] 我是否理解了用户的真实需求（而不是自己猜的那个）？
- [ ] 是否有多个可能的实现方向？我列出来了吗？
- [ ] 是否有更简单的方案？我反驳了吗？
- [ ] 是否有说不清的地方？我停下来问了吗？

---

## 原则 2：Simplicity First（能简单就简单）

**针对问题 P2：LLM 非常喜欢把代码和 API 写复杂，抽象层堆一堆，不清理死代码，100 行能搞定的事写成 1000 行。**

### 子规则

1. **不做超出需求的功能**（YAGNI：You Aren't Gonna Need It）。
2. **不给只用一次的代码写抽象层**。
3. **不写没人要的「灵活性」或「配置化」**。
4. **不给不可能发生的场景写错误处理**。
5. **200 行能改成 50 行，就重写**。

### 实用性测试

> 如果一个资深工程师看到这段代码会说「太复杂了」，那就简化。

### 与 go-code-standards.mdc 的关系

Simplicity First 是 `go-code-standards.mdc` 中以下规则的**行为来源**：
- 函数长度不超过 50 行
- 避免过度抽象
- 不重复造轮子
- 优先考虑可读性而非聪明性

---

## 原则 3：Surgical Changes（外科手术式改动）

**针对问题 P3：LLM 会顺手改掉或删掉自己没完全理解的注释和代码，哪怕和当前任务完全无关。**

### 子规则

1. **不要「改进」不在任务里的邻近代码、注释或格式**。
2. **不要重构没坏的东西**。
3. **即使不喜欢现有代码风格，也要沿用**。
4. **发现无关的死代码可以提一嘴，但不要自己删**。
5. **改出来的孤儿（orphan）要清理**。如果本次改动导致某些 import / 变量 / 函数变成了孤儿，要把它们清理掉。但预设存在的死代码不要动。

### 实用性测试

> **每一行改动都要能直接追溯到用户的需求。**

### 与 xiezhi 工作流的关系

贯穿所有阶段。diff 中每个变更都必须有明确的需求来源。

---

## 原则 4：Goal-Driven Execution（目标驱动执行）

**针对问题 P4：拿着模糊指令就开跑，做完不知道是不是真的做完。**

### 子规则

1. **把命令式任务转成可验证的目标**。不是说「加一个校验」，而是说「先为非法输入写测试，然后让测试通过」。
2. **目标必须可验证**。如果无法自动验证，至少要定义清楚「完成」的定义是什么。
3. **多步任务要写 plan 并标注验证方式**：

```
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

4. **这不是「必须先写测试」，而是「必须有一个可以被验证的成功标准」**。测试先行是最常见方式，但不是唯一形式。

### 命令式 vs 目标驱动对照

| 不要说 | 要说 |
|--------|------|
| 加一个校验 | 先为非法输入写测试，然后让测试通过 |
| 修这个 bug | 先写一个能复现 bug 的测试，然后让它通过 |
| 重构 X | 确保重构前后测试都通过 |

### 核心哲学（Karpathy 原话）

> "LLMs are exceptionally good at looping until they meet specific goals. Don't tell it what to do, give it success criteria and watch it go."

**译：LLM 最擅长的就是朝着明确目标不停 loop；别告诉它怎么做，给它成功标准让它自己跑。**

### 执行时机

在 `xiezhi-workflow.mdc` 阶段 6「提交总结」之前执行，作为成功标准验证。

---

## 适用边界

### 什么时候需要全套？

- 非琐碎的代码改动
- 新增功能、重构模块、复杂逻辑修改
- 后果有成本（生产环境、数据相关、安全相关）

### 什么时候不需要全套？

- 琐碎任务：修错别字、变量改名、单行改动、配置文件微调
- 探索性脚本、前端样式调试、配置试错（核心是快速反馈，不是严谨）

**用判断。** 不是每个改动都需要 Think Before Coding 的完整流程。xiezhi 工作流和本约束都是为「非琐碎任务」设计的。

---

## 在 xiezhi 工作流中的嵌入位置

| xiezhi 阶段 | 嵌入本文件中的哪个原则 |
|-------------|----------------------|
| 阶段 1「编写」之前 | Think Before Coding |
| 阶段 1「编写」中 | Simplicity First |
| 贯穿所有阶段 | Surgical Changes |
| 阶段 5「测试评审」前 | Goal-Driven Execution（验证成功标准）|

---

## 效果检验信号

如果本约束在生效，应该能观察到：
- diff 里多余的改动变少了——只有要求的地方被改了
- 因为写太复杂而被重写的次数变少了——第一版就是简单的
- 澄清问题发生在实现之前——而不是出错之后
- PR 变干净了——没有顺手做的重构，没有「顺便改的」
